{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-node",
            "console": "integratedTerminal",
            "request": "launch",
            "name": "Run launch.json ",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "program": "${workspaceFolder}/src/app.ts",
            "outFiles": [
                "${workspaceFolder}/**/*.js"
            ]
        }
    ]
}